#!/bin/sh
# test versions

in=$(basename $0)
in=${in%.*} 

cwd=`pwd`
test -f /usr/bin/cygpath && cwd=`cygpath -m $cwd`

cd $1
./phenod -cqln dwkr=0 s=$cwd/$in.inp jraf_root=jroot
rm -f phenod.db

echo ""
echo "=========="
ls -1aR jroot
#rm -rf jroot

